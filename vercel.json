{
  "buildCommand": "prisma generate && prisma db push --accept-data-loss && tsx scripts/ensure-order-numbers.ts && next build",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/auto-cancel-orders",
      "schedule": "0 * * * *"
    }
  ]
}
